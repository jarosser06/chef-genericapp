---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04
  - name: ubuntu-14.04
  - name: centos-6.5
  - name: centos-7.0

suites:
  - name: apache
    run_list:
      - recipe[genericapp-test::apache]
    attributes:
  - name: nginx
    run_list:
      - recipe[genericapp-test::nginx]
    attributes:
