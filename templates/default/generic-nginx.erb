server {
  listen  80;
  server_name  <%= @server_name %>;
  error_log <%= @error_log %>;
  access_log <%= @access_log %>;

  location / {
    root  <%= @document_root %>;
    index index.html;
  }
}
